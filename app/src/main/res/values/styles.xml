<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RadingStyle" parent="@android:style/Widget.RatingBar">
        <!-- 定义星星图片 -->
        <item name="android:progressDrawable">@drawable/rating_bar</item>
        <!-- 根据自定义星星图片的大小,设置相应的值,否则可能显示不全 -->
        <item name="android:minHeight">15dp</item>
    </style>

    <style name="RadingStyle_Small" parent="@android:style/Widget.RatingBar">

        <item name="android:minHeight">12dp</item>
        <item name="android:progressDrawable">@drawable/rating_bar_small</item>
    </style>

    <style name="style_share_window_anim">
        <item name="android:windowEnterAnimation">@anim/anim_share_window_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_share_window_exit</item>

    </style>

    <style name="Theme.UMDefault" parent="android:style/Theme.NoTitleBar"></style>

    <!--评分textview Style-->
    <style name="RatingText_Style" >
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/color_tv_orange</item>
        <item name="android:textSize">14sp</item>

    </style>

    <!--textview Style-->
    <style name="Text_Style" >
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:textColor">@color/color_title_pressed</item>
        <item name="android:textSize">14sp</item>

    </style>

</resources>
